package com.workforce.pipeline.service;

public class UserService {
    /*public User register(User user) {
        // validate user
        // hash password (optional for your level)
        // save to database
        return userRepository.save(user);
    }
     */
}
